# Paper Trading Pilot Dashboard

**Template** — regenerate with:

```bash
python scripts/generate_paper_trading_dashboard.py --as-of-date YYYY-MM-DD
```

This file is overwritten by the dashboard generator when run against a live database.
